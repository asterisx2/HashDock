import React, {PropTypes} from 'react';

const Card =({tweet}) => {
    return (
        <div>

        </div>
);
};

Card.propTypes = {
    tweet: PropTypes.object.isRequired
};

export default Card;
